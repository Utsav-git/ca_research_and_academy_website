import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0090FF",
    },
    secondary: {
      main: "#FFD600",
    },
    background: {
      default: "#F5F8FA",
    },
    text: {
      primary: "#223142",
      secondary: "#7A869A",
    },
  },
});

export default theme;
