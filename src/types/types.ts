export interface NavItem {
  label: string;
  path: string;
}
export interface FeaturedServiceCardType {
  id: string;
  title: string;
  description: string;
}

export interface MarketInsightsType {
  id: string;
  headline: string;
  summary: string;
  date: string;
}
